# Uninstall

Remove the public SDK with `pip uninstall aegis-trial-sdk`. Remove the separately
installed Trial Core with `pip uninstall aegis-trial-core` and delete any
caller-selected local logs.
