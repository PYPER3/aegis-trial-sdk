# Privacy

The SDK processes observation data locally and writes only to caller-selected local
paths. It contains no telemetry client, analytics client, cloud inference client,
or phone-home licensing flow.
