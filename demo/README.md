# Trial preview

`aegis-demo --preview` verifies the local report format without a model or Trial
Core and labels the output as simulated. For live local detection, install the
separately supplied compatible Trial Core wheel and use the standard monitor
lifecycle documented in the repository README.

Do not add private implementation details, private corpora, or customer data to
this directory.
